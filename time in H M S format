#include<stdio.h>
int main(){
	int time;
	scanf("%d",&time);
	int hour = time / 3600;
	int minute = (time % 3600) / 60;
	int sec = (time % 3600) % 60;
	printf("%dH:%dM:%dS",hour, minute, sec);
	return 0;
}
